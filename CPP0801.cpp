#include<bits/stdc++.h>

#define ll long long
#define forl(i,a,b) for(int i=a; i<b; i++)
#define forb(i,a,b) for(int i=a; i<=b; i++)

using namespace std;

int main(){
	ifstream in("PTIT.in");
	ofstream out("PTIT.out");
	string str;
	while(in >>str)
		{
			out <<str <<endl;
		}
	in.close();
	out.close();
}
